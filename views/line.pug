extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action="users/abc", method="post")
    input(type="text", name="name")
    input(type="password", name="password")
    input(type="submit", value="submit")
